# Ralph Progress Log
# Project: AI Video Cataloger
# Branch: ralph/ai-video-cataloger-mvp
# Started: 2026-01-23

## Codebase Patterns
- Use sql.js instead of better-sqlite3 (native module compatibility with Node.js v25)
- ESM modules ("type": "module" in package.json)
- TypeScript strict mode enabled
- Entry point: src/index.ts with shebang
- Use execa for running shell commands (already a dependency)
- Use chalk for colored terminal output (already a dependency)
- Services are exported through src/services/index.ts barrel export
- Database exports through src/db/index.ts barrel export
- Utilities are exported through src/utils/index.ts barrel export
- Database file stored in .ai-video-cataloger/catalog.db (auto-created)
- Call saveDatabase() after each database modification (sql.js is in-memory)
- Video extensions: .mp4, .mov, .avi, .mkv, .webm
- Use fluent-ffmpeg for video/audio operations (ffprobe for metadata, ffmpeg for extraction)
- Frame/transcript/summary directories stored in same directory as videos (e.g., frames/{video-name}/)
- Temp audio files stored in OS temp directory using getTempAudioPath(videoPath)
- Claude CLI for analysis: `claude -p <prompt> <image1> <image2> ...` to pass frames as attachments
- Analysis response format: DESCRIPTION: <text> FILENAME: <kebab-case-name>

---

## 2026-01-23 - US-001
- What was implemented: Project scaffolding with TypeScript and all required dependencies
- Files changed:
  - package.json (created with dependencies)
  - tsconfig.json (created with strict mode)
  - .gitignore (created)
  - src/index.ts (entry point with shebang)
  - src/types/index.ts (type definitions)
  - src/services/index.ts (barrel export)
  - src/db/index.ts (barrel export)
  - src/utils/index.ts (barrel export)
- **Learnings for future iterations:**
  - better-sqlite3 has compatibility issues with Node.js v25 (native module compilation fails), use sql.js instead
  - fluent-ffmpeg is deprecated but still works for basic operations
  - Directory structure: src/, src/services/, src/db/, src/utils/, src/types/
---

## 2026-01-23 - US-002
- What was implemented: Prerequisites checker to verify all required dependencies on startup
- Files changed:
  - src/services/prerequisites.ts (created - checks Node.js, ffmpeg, Claude CLI, Whisper)
  - src/services/index.ts (updated - export checkPrerequisites)
  - src/index.ts (updated - call checkPrerequisites on startup, skip for --help/--version)
- **Learnings for future iterations:**
  - Use execa for running shell commands to check for binaries
  - Whisper doesn't have a --version flag, just check if --help runs successfully
  - Claude CLI version output includes full string like "2.1.9 (Claude Code)"
  - ffmpeg version can be parsed from first line of `ffmpeg -version` output
  - Skip checks for help/version by checking process.argv before calling checkPrerequisites
---

## 2026-01-23 - US-003
- What was implemented: SQLite database service using sql.js for tracking video processing progress
- Files changed:
  - src/db/database.ts (created - database initialization, CRUD operations for videos and config)
  - src/db/index.ts (updated - export all database functions)
  - src/index.ts (updated - call initDatabase on startup, register cleanup handler)
  - package.json (added @types/sql.js devDependency)
- **Learnings for future iterations:**
  - sql.js uses async initialization with `initSqlJs()` to load the WASM module
  - Database needs to be exported and written to file after each change (in-memory only by default)
  - Use `db.exec()` for queries that return results, `db.run()` for writes
  - Result format from db.exec() is `{ columns: string[], values: any[][] }` - need helper to convert to objects
  - Register cleanup handler with `process.on('exit', ...)` to ensure database is saved before exit
---

## 2026-01-23 - US-004
- What was implemented: Directory scanner to find video files and register them in database
- Files changed:
  - src/services/scanner.ts (created - scans directories for video files, registers in database)
  - src/utils/hash.ts (created - SHA-256 file hashing with streaming)
  - src/services/index.ts (updated - export scanDirectory and ScanResult)
  - src/utils/index.ts (updated - export hashFile)
  - src/index.ts (updated - integrate scanner, handle directory argument)
- **Learnings for future iterations:**
  - Use createReadStream for hashing large files to avoid memory issues
  - Video extensions: .mp4, .mov, .avi, .mkv, .webm
  - getVideosByStatus('completed') returns completed videos to skip
  - Scanner returns ScanResult with newVideos array, skippedCompleted count, and totalFound
  - First positional argument (non-flag) is treated as directory path
---

## 2026-01-23 - US-005
- What was implemented: Frame extraction service using fluent-ffmpeg to extract frames from videos
- Files changed:
  - src/services/frames.ts (created - extractFrames function, getFramesDir helper)
  - src/services/index.ts (updated - export extractFrames and getFramesDir)
  - src/index.ts (updated - integrate frame extraction into video processing loop)
- **Learnings for future iterations:**
  - Use fluent-ffmpeg's ffprobe to get video duration metadata
  - Extract frames at evenly distributed positions (25%, 50%, 75% for 3 frames) using formula: duration * (i / (frameCount + 1))
  - Use seekInput() to seek to timestamp before extracting frame (more efficient than seeking during output)
  - Frames saved to frames/{video-name}/frame-001.jpg pattern
  - ora spinner provides nice UX during long-running frame extraction
  - getFramesDir() helper useful for later stories that need to reference frames location
---

## 2026-01-23 - US-006
- What was implemented: Audio extraction service using fluent-ffmpeg to extract audio tracks from videos
- Files changed:
  - src/services/audio.ts (created - extractAudio, getTempAudioPath, getTempAudioDir, cleanupTempAudio)
  - src/services/index.ts (updated - export audio extraction functions)
  - src/index.ts (updated - integrate audio extraction into video processing loop after frame extraction)
- **Learnings for future iterations:**
  - Use ffprobe to check for audio streams with `metadata.streams.filter(s => s.codec_type === 'audio')`
  - Extract audio with fluent-ffmpeg: noVideo(), audioCodec('pcm_s16le'), audioFrequency(16000), audioChannels(1) for Whisper compatibility
  - Temp audio files stored in OS temp directory: `os.tmpdir()/ai-video-cataloger/audio/{video-name}.wav`
  - cleanupTempAudio() function available for cleanup after transcription (US-014)
  - getTempAudioPath() allows retrieving audio path by video path for transcription step
---

## 2026-01-23 - US-007
- What was implemented: Transcription service using local Whisper to transcribe audio from videos
- Files changed:
  - src/services/transcription.ts (created - transcribeAudio, getTranscriptsDir, getTranscriptPath)
  - src/services/index.ts (updated - export transcription functions)
  - src/index.ts (updated - integrate transcription into video processing loop after audio extraction)
- **Learnings for future iterations:**
  - Whisper CLI: `whisper <audio_file> --model base --output_dir <dir> --output_format txt`
  - Whisper outputs file with same basename as input audio file (e.g., video-name.wav -> video-name.txt)
  - Transcripts stored in transcripts/{video-name}.txt in same directory as videos
  - Pass hasAudio flag from audio extraction step to skip transcription for silent videos
  - Videos without audio tracks still move to 'transcribed' status (graceful skip)
---

## 2026-01-23 - US-008
- What was implemented: Video analyzer service using Claude Code CLI to analyze frames and transcripts
- Files changed:
  - src/services/analyzer.ts (created - analyzeVideo, getSummariesDir, getSummaryPath, parseClaudeResponse)
  - src/services/index.ts (updated - export analyzer functions)
  - src/index.ts (updated - integrate analyzer into video processing loop after transcription)
- **Learnings for future iterations:**
  - Claude Code CLI: `claude -p <prompt> <image1> <image2> ...` to pass frames as attachments
  - Parse Claude's response using DESCRIPTION: and FILENAME: markers
  - Summaries stored in summaries/{video-name}.txt in same directory as videos
  - Pass hasTranscript flag from transcription step to conditionally include transcript in prompt
  - Clean up suggested filenames to ensure valid kebab-case (lowercase, no special chars)
  - transcribeAudio returns TranscriptionResult with transcribed boolean, need to capture it for analyzer
---

## 2026-01-23 - US-009
- What was implemented: Video renamer service that renames videos with descriptive names based on Claude's analysis
- Files changed:
  - src/services/renamer.ts (created - renameVideo, sanitizeSlug, getUniqueFilename, getDatePrefix)
  - src/services/index.ts (updated - export renameVideo and RenameResult)
  - src/index.ts (updated - integrate renamer into video processing loop after analysis)
- **Learnings for future iterations:**
  - Use date-fns format() for date formatting: `format(stats.mtime, 'yyyy-MM-dd')`
  - Get file modification time with statSync(filePath).mtime
  - When renaming associated files, build the new path based on new video basename (not old)
  - Conflict resolution: loop with counter until finding unique filename (baseName-2.ext, baseName-3.ext, etc.)
  - Always update both new_name and status in database after successful rename
  - Store analysis.suggestedFilename from analyzeVideo() result to pass to renamer
---

## 2026-01-23 - US-010
- What was implemented: Resume interrupted processing support for batch video processing
- Files changed:
  - src/services/analyzer.ts (updated - added getSuggestedFilenameFromSummary to extract filename from saved summary)
  - src/services/scanner.ts (updated - separate newVideos and resumingVideos in ScanResult)
  - src/services/index.ts (updated - export getSuggestedFilenameFromSummary)
  - src/index.ts (updated - refactored into processVideo function, handle resume from any state)
- **Learnings for future iterations:**
  - ScanResult now has separate arrays: newVideos (pending), resumingVideos (in progress)
  - When resuming from 'analyzed' status, retrieve suggestedFilename from summary file using getSuggestedFilenameFromSummary()
  - Scanner displays resume count with status of each resuming video in yellow
  - getIncompleteVideos() in database.ts already exists for querying videos not in completed/error status
  - Summary file format: SUGGESTED FILENAME:\n<filename> - use regex to extract
---

## 2026-01-23 - US-011
- What was implemented: Basic CLI interface using commander.js with options for frames, skip-rename, verbose
- Files changed:
  - src/index.ts (refactored - replaced manual arg parsing with commander.js, added CLI options handling)
- **Learnings for future iterations:**
  - commander.js is already a dependency, use it for CLI argument parsing
  - Read package.json version dynamically: `JSON.parse(readFileSync(join(__dirname, '..', 'package.json'), 'utf-8')).version`
  - Use `.option('-f, --frames <number>', ...)` for options with values
  - Use `.option('-s, --skip-rename', ...)` for boolean flags (default: false)
  - Global options object pattern works well for passing CLI options to helper functions
  - logVerbose() helper function checks cliOptions.verbose before logging
  - When --skip-rename is set, call updateVideoStatus() directly instead of renameVideo()
---

## 2026-01-23 - US-012
- What was implemented: Progress display showing current video position and step, with final summary
- Files changed:
  - src/index.ts (updated - added ProcessingStats interface, getProgressPrefix, logStep, displaySummary functions)
- **Learnings for future iterations:**
  - Individual services (frames.ts, audio.ts, transcription.ts, analyzer.ts, renamer.ts) already use ora spinners
  - Track processing stats with a global interface: totalVideos, currentIndex, processedCount, errorCount, errors array
  - Use chalk.bold() for visual separators in summary display
  - Progress prefix "[X/Y]" shown before each step with logStep() helper
  - displaySummary() shows final counts and lists specific error messages for failed videos
  - Error tracking allows for better debugging without stopping the batch process
---

## 2026-01-23 - US-013
- What was implemented: Error handling and recovery with --retry-errors flag to re-process failed videos
- Files changed:
  - src/index.ts (updated - added updateVideoStatus call on error, added retryErrors CLI option)
  - src/services/scanner.ts (updated - added ScanOptions, handle errored videos, added retryingVideos and skippedErrors tracking)
  - src/services/index.ts (updated - export ScanOptions type)
- **Learnings for future iterations:**
  - updateVideoStatus(id, 'error', errorMessage) already supports error message as 3rd parameter
  - Scanner needs to handle errored videos separately from completed videos
  - When retrying errored videos, reset their status to 'pending' so they start from the beginning
  - ScanResult now includes retryingVideos and skippedErrors fields
  - Use chalk.magenta for retry-related messages to distinguish from resume (yellow) and new (green)
  - Errored videos are added to the front of the processing queue (retrying first, then resuming, then new)
---

## 2026-01-23 - US-014
- What was implemented: Cleanup of temporary audio WAV files after transcription completes
- Files changed:
  - src/index.ts (updated - import cleanupTempAudio and getTempAudioPath, call cleanup after transcription and on error)
- **Learnings for future iterations:**
  - cleanupTempAudio() function was already implemented in src/services/audio.ts (US-006), just needed to be used
  - Call cleanup immediately after transcription completes (when audio file is no longer needed)
  - Call cleanup in error handler for best-effort cleanup on failure
  - cleanupTempAudio() handles non-existent files gracefully (no error thrown)
  - Log cleanup action in verbose mode for debugging
---

## 2026-01-23 - US-015
- What was implemented: Claude Analysis Timeout Handling with configurable timeout and elapsed time display
- Files changed:
  - src/services/analyzer.ts (updated - added timeout support with execa, elapsed time spinner, timeout error handling)
  - src/services/index.ts (updated - export AnalysisOptions type)
  - src/index.ts (updated - added --timeout CLI flag, pass timeout to analyzeVideo)
- **Learnings for future iterations:**
  - execa supports timeout option: `execa('command', args, { timeout: timeoutMs })` - timeout in milliseconds
  - execa timeout errors have `timedOut: boolean` property to check if error was a timeout
  - Use setInterval to update spinner text with elapsed time every second
  - Always clearInterval in both success and error paths to avoid memory leaks
  - Timeout errors should throw a new Error with clear message like "Analysis timed out after X seconds"
  - CLI options with value parsers: `.option('-t, --timeout <seconds>', 'description', (value) => parseInt(value, 10), 120)`
---

## 2026-01-23 - US-016
- What was implemented: Whisper Mode Selection for flexible transcription options
- Files changed:
  - src/types/index.ts (added WhisperMode type: 'local' | 'api' | 'skip')
  - src/services/prerequisites.ts (added PrerequisiteOptions interface, conditional Whisper check)
  - src/services/index.ts (export PrerequisiteOptions type)
  - src/index.ts (added --whisper and --skip-transcribe flags, API key validation)
- **Learnings for future iterations:**
  - Use commander value parser for validation: `.option('-w, --whisper <mode>', ..., (value) => { validate; return value as Type; }, 'default')`
  - Handle alias flags by checking for both in action: `const mode = options.skipTranscribe ? 'skip' : options.whisper`
  - Validate environment variables early in run() before checkPrerequisites for clear error messages
  - checkPrerequisites can accept optional PrerequisiteOptions to conditionally skip checks
  - Use chalk.blue('○') for skipped/not-applicable checks in prerequisites output
---
